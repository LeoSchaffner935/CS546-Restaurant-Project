<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Quick Bites</title>
        <link rel="stylesheet" href="../../public/site.css">
    </head>
    <body>
        <header>
            <div id="navbar">
                <h3>Quick Bites</h3>
                <a href="/">Home</a>
                <a href="/restaurants">Restaurants</a>
                {{#if authenticated}}
                <a href='/private'>My Account</a>
                <a href='/logout'>Logout</a>
                {{/if}}
                {{#unless authenticated}}
                <a href="/login">Login</a>
                <a href="/signup">Signup</a>
                {{/unless}}
                <a href="/requestR">Request Restaurant</a>

                <form id="searchForm" action="/search" method="POST">
                    <label>Search
                        <input id="searchTerm" name="searchTerm" type="text" placeholder="Search here...">
                    </label>
                    <button type="submit">Search</button>
                </form>
            </div>
        </header>
        <main>{{{body}}}</main>
        <footer>
            <p class= "credits">


                Farid Jafri
                Ryan Gray
                Leo Schaffner
                Ziyou Shang
            </p>
        </footer>
    </body>
</html>
