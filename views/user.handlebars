<div id="userInfo">
    <h1>{{user.username}}</h1>
    <p>{{user.firstName}} {{user.lastName}}</p>
    <p>Bio: {{user.bio}}</p>
    <div id="userReviews">
        <h2>Reviews</h2>
        {{#each user.reviews}}
            <dl id="userReview">
                <dt>Title</dt>
                <dd>{{this.title}}</dd>

                <dt>Restaurant</dt>
                <dd>{{this.restaurantReviewed}}</dd> <!-- Add link to restaurant -->

                <dt>Rating</dt>
                <dd>{{this.rating}}</dd>

                <dt>Date</dt>
                <dd>{{this.dateOfReview}}</dd>

                <dt>Tags</dt>
                <dd><ul></ul></dd>
                {{#each this.tags}}
                    <li>{{this}}</li>
                {{/each}}

                <p>{{this.content}}</p>
            </dl>
        {{/each}}
    </div>
    <div id="userComments">
        <h2>Comments</h2>
        {{#each user.comments}}
            <dl id="userComment">
                <dt>Date</dt>
                <dd>{{this.date}}</dd>

                <dt>Comment</dt>
                <dd>{{this.comment}}</dd>
            </dl>
        {{/each}}
    </div>
</div>